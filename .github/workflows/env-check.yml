name: Env Check Workflow
on: [push]

# 【ここで環境変数を作成・定義します】
env:
  GREETING_MSG: "これは環境変数（env）から呼び出されたメッセージです！"

jobs:
  env-job:
    runs-on: ubuntu-latest
    steps:
      - name: 環境変数（env）の確認
        # ${{ env.変数名 }} で環境変数の値を動的に呼び出す
        run: |
          echo "メッセージ: ${{ env.GREETING_MSG }}"